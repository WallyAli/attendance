<h2>Child's detail information</h2>

<%= @child.first_name %> <%= @child.last_name %></br>
<%= @child.day %>




<p>
<%= link_to 'create schedule', new_schedule_path(
@child) %> ||

<%= link_to 'Delete', child_path(@child), method: 
            :delete, data: { confirm: 'Are you sure?' }  %>
</p>


